name: Node.js CI/CD to AWS EC2

on:
  push:
    branches:
      - master # Trigger on pushes to the main branch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Login to docker hub
        uses: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - name: Build docker images
        uses: docker build -t himanshugoelnasa/mern-node .
      - name: Puiblish image to docker hub
        uses: pocker push himanshugoelnasa/mern-node:latest




  deploy:
    needs: build
    runs-on: self-hosted
    steps:
      - name: pull image from docker hub
        uses: docker pull himanshugoelnasa/mern-node:latest
      - name: delete old container
        uses: docker rm -f mern-node-container
      - name: run docker container
        uses: docker run -d -p 5000:5000 --name mern-node-container himanshugoelnasa/mern-node
  